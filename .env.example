ANTHROPIC_API_KEY=sk-ant-...
DB_PASSWORD=govai_secret
LOG_LEVEL=INFO
JWT_SECRET=wygeneruj-przez-python-c-import-secrets-print-secrets-token-hex-32
# CORS — domena(y) konsoli, oddzielone przecinkiem (prod: tylko domena kancelarii/klienta)
ALLOWED_ORIGINS=https://localhost
# Domena publiczna (Caddy) — ustaw dla realnego wdrożenia, żeby dostać automatyczny cert ACME.
# Bez tego Caddy nasłuchuje na :443 z samopodpisanym certyfikatem (OK dla lokalnego dev/INT).
DOMAIN_NAME=
